# Set the path to your copy of the GOV2 corpus here. This is used to build the
# index and extract features
# (e.g. replace /GOV2_CORPUS with an actual path to the Gov2 corpus)
CORPUS_PATH="/GOV2_CORPUS"
